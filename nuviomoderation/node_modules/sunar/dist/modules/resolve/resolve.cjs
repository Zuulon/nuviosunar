'use strict';

var path = require('path');
var glob = require('glob');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
async function resolve(patterns, options = {}) {
  const paths = await glob.glob(patterns, options);
  const imports = paths.map((path$1) => {
    const absolute = path.join(process.cwd(), path$1.toString());
    return import(`file://${absolute}`);
  });
  return Promise.all(imports);
}
__name(resolve, "resolve");

exports.resolve = resolve;
//# sourceMappingURL=resolve.cjs.map
//# sourceMappingURL=resolve.cjs.map