'use strict';

var index_cjs = require('../../utils/index.cjs');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
async function registerGuildCommands(application, guildIds) {
  const applicationCommands = index_cjs.getApplicationCommands();
  if (applicationCommands.length <= 0) return [];
  const results = [];
  for (const guildId of guildIds) {
    const result = await application.commands.set(applicationCommands, guildId);
    results.push(result);
  }
  return results;
}
__name(registerGuildCommands, "registerGuildCommands");

exports.registerGuildCommands = registerGuildCommands;
//# sourceMappingURL=guild.cjs.map
//# sourceMappingURL=guild.cjs.map