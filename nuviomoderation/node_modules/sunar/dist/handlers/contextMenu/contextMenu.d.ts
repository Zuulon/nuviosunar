import { UserContextMenuCommandInteraction, MessageContextMenuCommandInteraction } from 'discord.js';

/**
 * Handle a context menu interaction.
 * @param interaction The context menu interaction to handle
 *
 * @see https://sunar.js.org/docs/guides/interactions-handling
 */
declare function handleContextMenu(interaction: UserContextMenuCommandInteraction | MessageContextMenuCommandInteraction): Promise<void>;

export { handleContextMenu };
