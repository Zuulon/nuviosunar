'use strict';

var index_cjs$1 = require('../index.cjs');
var index_cjs = require('../../stores/index.cjs');
var index_cjs$2 = require('../protectors/index.cjs');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
async function handleContextMenu(interaction) {
  const command = index_cjs.contextMenuCommands.get(interaction.commandName);
  if (!command) return;
  const onCooldown = index_cjs$1.handleCooldown(interaction, command);
  if (onCooldown) return;
  if (typeof command.execute !== "function") return;
  const canContinue = await index_cjs$2.handleProtectors({ protectors: command.protectors, data: interaction });
  if (!canContinue) return;
  const result = await command.execute(interaction);
  if (!result) return;
}
__name(handleContextMenu, "handleContextMenu");

exports.handleContextMenu = handleContextMenu;
//# sourceMappingURL=contextMenu.cjs.map
//# sourceMappingURL=contextMenu.cjs.map