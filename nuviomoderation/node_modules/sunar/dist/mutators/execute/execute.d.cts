import { B as Builder } from '../../builder-C6J45hMw.cjs';
import '../../client-CcTd37MH.cjs';
import 'discord.js';
import '../../utils/constants.cjs';
import '../../utils/enums.cjs';
import '../../types/utils.cjs';

type ExecutableBuilder = Pick<Builder, 'type' | 'execute'>;
/**
 * Set the function to execute when an executable builder is accepted.
 *
 * @param builder The builder to mutate
 * @param execute Callback function to execute when a builder is accepted
 *
 * @see https://sunar.js.org/docs/mutators/execute
 */
declare function execute<TBuilder extends ExecutableBuilder>(builder: TBuilder, execute: TBuilder['execute']): void;

export { type ExecutableBuilder, execute };
