import { ChatInputCommandInteraction, Awaitable } from 'discord.js';
import { B as Builder, a as Protector } from '../builder-C6J45hMw.cjs';
import { CooldownProp } from '../types/config.cjs';
import '../client-CcTd37MH.cjs';
import { Builders } from '../utils/enums.cjs';
import '../utils/constants.cjs';
import '../types/utils.cjs';

interface GroupConfig extends CooldownProp {
}
/**
 * The Group class handles slash commands with subcommands, allowing for structured and efficient
 * management of hierarchical commands under a single root.
 *
 * @see https://sunar.js.org/docs/builders/group
 */
declare class Group implements Builder {
    readonly type = Builders.Group;
    root: string;
    parent: string;
    sub?: string;
    config: GroupConfig;
    protectors: Protector<{
        commands: 'slash'[];
    }>[];
    execute: (interaction: ChatInputCommandInteraction) => Awaitable<unknown>;
    constructor(root: string, parent: string, sub?: string);
}

export { Group, type GroupConfig };
