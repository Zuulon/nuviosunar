import { ChatInputApplicationCommandData, ChatInputCommandInteraction, Awaitable } from 'discord.js';
import { B as Builder, a as Protector } from '../builder-waQmx6ft.js';
import { CommandConfig } from '../types/config.js';
import '../client-CVkzvYIb.js';
import { Builders } from '../utils/enums.js';
import '../utils/constants.js';
import '../types/utils.js';

interface SlashConfig extends CommandConfig {
}
/**
 * Slash commands are one of the primary ways users interact with bots. They provide a structured way for users to issue commands directly within the chat interface.
 *
 * @see https://sunar.js.org/docs/builders/slash
 */
declare class Slash implements Builder {
    readonly type = Builders.Slash;
    readonly data: ChatInputApplicationCommandData;
    config: SlashConfig;
    protectors: Protector<{
        commands: 'slash'[];
    }>[];
    execute: (interaction: ChatInputCommandInteraction) => Awaitable<unknown>;
    constructor(data: ChatInputApplicationCommandData);
}

export { Slash, type SlashConfig };
