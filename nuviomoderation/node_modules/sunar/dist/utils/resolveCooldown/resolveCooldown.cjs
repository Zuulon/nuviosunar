'use strict';

var index_cjs = require('../../index.cjs');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
function resolveCooldown(cooldown) {
  const isNumber = typeof cooldown === "number";
  const isGlobalScope = !isNumber && cooldown.scope === index_cjs.CooldownScope.Global;
  let time;
  let scope = index_cjs.CooldownScope.User;
  let limit = 1;
  let exclude = [];
  if (isNumber) {
    time = cooldown;
  } else {
    if (cooldown.scope) scope = cooldown.scope;
    if (cooldown.limit) limit = cooldown.limit;
    time = cooldown.time;
    if (!isGlobalScope) {
      exclude = cooldown.exclude ?? [];
    }
  }
  return { time, limit, scope, exclude };
}
__name(resolveCooldown, "resolveCooldown");

exports.resolveCooldown = resolveCooldown;
//# sourceMappingURL=resolveCooldown.cjs.map
//# sourceMappingURL=resolveCooldown.cjs.map